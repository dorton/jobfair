<%= simple_form_for @event do |f| %>


  <%= f.input :name %>
  <%= f.input :event_type, collection: ['Crash Course', 'Open House', 'Demo Day', 'Presentation Day'] %>

  <%= f.input :date %>

  <%= f.button :submit %>
<% end %>
